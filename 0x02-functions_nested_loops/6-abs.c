#include "main.h"
/**
 * _abs - write a function that print gthe sign of a number
 * @n: argument to be check
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
